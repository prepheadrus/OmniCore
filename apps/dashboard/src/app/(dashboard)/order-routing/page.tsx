import OrderRoutingClient from '../../../components/order-routing/page-client';

export const metadata = {
  title: 'Akıllı Sipariş Yönlendirme | Omnicore',
};

export default function OrderRoutingPage() {
  return <OrderRoutingClient />;
}