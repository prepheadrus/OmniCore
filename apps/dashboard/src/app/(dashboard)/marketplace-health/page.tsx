import MarketplaceHealthClient from '../../../components/marketplace-health/page-client';

export const metadata = {
  title: 'Pazar Yeri Sağlığı | Omnicore',
};

export default function MarketplaceHealthPage() {
  return <MarketplaceHealthClient />;
}