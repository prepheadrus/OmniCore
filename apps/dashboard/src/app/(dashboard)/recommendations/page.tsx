import RecommendationsClient from '../../../components/recommendations/page-client';

export const metadata = {
  title: 'Akıllı Öneri Motoru | Omnicore',
};

export default function RecommendationsPage() {
  return <RecommendationsClient />;
}