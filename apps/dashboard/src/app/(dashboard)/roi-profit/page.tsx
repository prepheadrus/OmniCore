import ROIProfit from '../../../components/roi-profit';

export const metadata = {
  title: 'ROI & Kar Analizi | Dashboard',
};

export default function ROIProfitPage() {
  return (
    <div className="py-6">
      <ROIProfit />
    </div>
  );
}
