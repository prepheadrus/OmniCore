import ContentRules from '../../../components/content-rules';

export const metadata = {
  title: 'İçerik Optimizasyonu | Dashboard',
};

export default function ContentRulesPage() {
  return (
    <div className="py-6">
      <ContentRules />
    </div>
  );
}
