import ReconciliationClient from '../../../components/reconciliation/page-client';

export const metadata = {
  title: 'Finansal Mutabakat | Omnicore',
};

export default function ReconciliationPage() {
  return <ReconciliationClient />;
}