import TemplateDesignerClient from '../../../components/template-designer/page-client';

export const metadata = {
  title: 'Etiket Şablonları | Omnicore',
};

export default function TemplateDesignerPage() {
  return <TemplateDesignerClient />;
}